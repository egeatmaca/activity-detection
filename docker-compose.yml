version: '3'

services:
  activity-detection:
    image: egeatmaca/activity-detection
    volumes: 
      - .:/usr/src/app
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
    ports: 
      - 5001:80
    devices:
      - /dev/video0:/dev/video0
    environment:
      - "DISPLAY=$DISPLAY"
